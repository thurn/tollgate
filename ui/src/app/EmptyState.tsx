import { motion } from "framer-motion";
import { ArrowRight, Command, FolderGit2, GitCommitHorizontal, ShieldCheck, Sparkles, TerminalSquare } from "lucide-react";
import { Button } from "../components/ui/Button";

export function EmptyState({ onOpen }: { onOpen: () => void }) {
  return <main className="onboarding"><div className="onboarding__glow" /><motion.div initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ type: "spring", stiffness: 330, damping: 30 }}><div className="onboarding__mark"><span /><span /><span /><ShieldCheck /></div><div className="eyebrow"><Sparkles size={13} />LOCAL CI, EXACT PROMOTION</div><h1>Your commits deserve<br />a proper <em>tollgate.</em></h1><p>Validate speculative Git prefixes in parallel, then promote only the exact objects that passed. No cloud. No daemon. No surprises.</p><div className="onboarding__actions"><Button variant="primary" onClick={onOpen}><FolderGit2 size={16} />Open a repository</Button><button><TerminalSquare size={15} />Initialize with <code>tg init</code></button></div><div className="onboarding__flow"><div><span><GitCommitHorizontal /></span><p><strong>Approve</strong><small>Capture one clean commit</small></p></div><ArrowRight /><div><span><Command /></span><p><strong>Validate</strong><small>Test the exact prefix</small></p></div><ArrowRight /><div><span><ShieldCheck /></span><p><strong>Promote</strong><small>Atomic release CAS</small></p></div></div></motion.div><footer>Runs with your local toolchain · macOS Apple Silicon · no telemetry</footer></main>;
}
